def solve lists_of_lists counter = for array in lists_of_lists for el in array if el not in counter counter el = 1 else counter el += 1 missing = el for el in counter if counter el % 2 == 1 return  join str x for x in sorted missing 		1 10
