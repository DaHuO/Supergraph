def main inputs = for line in fileinput input inputs append line strip t = int inputs 0 start = 1 for t in xrange t n = int inputs start heights = inputs start + 1 start + n * 2 start += n * 2 res = run n heights print case #%d: %s % t + 1  join res strip 		19 34
def run n heights s = set for row in heights for c in row split h = int c if h in s s remove h else s add h l = map str sorted list s return l 		4 16
