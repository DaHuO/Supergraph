def dfs now order if visited now != 0 if visited now >= start tmp = order - visited now circle now = tmp return now true order else return label now false order visited now = order start_of_circle in_circle last_order = dfs a now order + 1 if in_circle == false label now = start_of_circle length now = length a now + 1 return start_of_circle false last_order else label now = now length now = 0 if start_of_circle == now return start_of_circle false last_order else return start_of_circle true last_order 		4 35
