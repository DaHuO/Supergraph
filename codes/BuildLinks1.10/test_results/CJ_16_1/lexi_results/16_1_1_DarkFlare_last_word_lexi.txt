def construct_last_word original if original == return str_list = str_list extend original str_list reverse max_ch = str_list 0 for ch in str_list if ch > max_ch max_ch = ch index = str_list index max_ch str_list reverse index = len str_list - index - 1 right = str_list 0 index if index == len str_list - 1 left = else left = str_list index + 1 if len right == 0 return str max_ch + join left elif len left == 0 return str max_ch + construct_last_word right return str max_ch + construct_last_word right + join left 		4 22
