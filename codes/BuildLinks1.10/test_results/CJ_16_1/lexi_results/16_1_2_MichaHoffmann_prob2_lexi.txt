def solve lists merged = list itertools chain from_iterable lists menge = list set merged solution = for k in menge if merged count k % 2 == 1 solution append k strsolution = str k for k in sorted solution return  join strsolution 		3 11
