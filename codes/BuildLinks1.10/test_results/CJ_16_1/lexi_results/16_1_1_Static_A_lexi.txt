def solve line string = line split 0 size = len string all_strings = string 0 for i in range 1 size other_str = copy copy all_strings for j in range len all_strings all_strings j += string i for k in range len other_str temp = other_str k other_str k = string i + temp all_strings += other_str all_strings sort return all_strings len all_strings - 1 		16 30
def writestringtofile output filename out_file = open filename + .out w for i in output out_file write i + \n out_file close 		10 14
def load_file filename in_file = open filename encoding = utf-8 content = in_file readlines return content 		5 8
