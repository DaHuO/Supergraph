def main numberofcases = int input count = 1 for x in range numberofcases ans = dictionary = sizeofmatrix = int input for x in range 2 * sizeofmatrix - 1 temp = input temp = temp split for each in temp if int each in dictionary dictionary int each += 1 else dictionary int each = 1 for each in dictionary keys if dictionary each % 2 != 0 ans append each ans sort ans =  join str tempstr for tempstr in ans print case #%d: %s % count ans count += 1 		1 26
