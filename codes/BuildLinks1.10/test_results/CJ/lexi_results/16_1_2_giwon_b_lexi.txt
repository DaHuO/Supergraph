def get_missing_row rowlen rowcol_lines logger info rowlen: %d rowlen hcnts = dict for line in rowcol_lines line = line strip row = list for height in line split  height = int height if height in hcnts hcnts height += 1 else hcnts height = 1 missing_row = list for h cnt in hcnts iteritems if cnt % 2 == 1 missing_row append h assert len missing_row == rowlen missing_row sort missing_row = str h for h in missing_row res =  join missing_row return res 		12 34
