def find_optimal_scores c j digits_cnt = len c numbers = {:03d} format num - digits_cnt for num in range 10 ** digits_cnt c_re = re compile c replace ? . j_re = re compile j replace ? . c_options = num for num in numbers if c_re match num j_options = num for num in numbers if j_re match num min_diff = 1000 min_c = min_j = for c in c_options for j in j_options diff = abs int c - int j if diff < min_diff min_c = c min_j = j min_diff = diff return min_c min_j 		4 21
