def process line digits = digits append zero digits append one digits append two digits append three digits append four digits append five digits append six digits append seven digits append eight digits append nine remaindigits = remaindigits append one remaindigits append three remaindigits append five remaindigits append seven remaindigits append nine numbers = while line != if z in line digit = zero if checkword digit line line = removedigit digit line no = digits index digit numbers append no elif w in line digit = two if checkword digit line line = removedigit digit line no = digits index digit numbers append no elif u in line digit = four if checkword digit line line = removedigit digit line no = digits index digit numbers append no elif x in line digit = six if checkword digit line line = removedigit digit line no = digits index digit numbers append no elif g in line digit = eight if checkword digit line line = removedigit digit line no = digits index digit numbers append no else for digit in remaindigits if checkword digit line line = removedigit digit line no = digits index digit numbers append no numbers_str = numbers sort for no in numbers numbers_str += str no return numbers_str 		30 107
def checkword digit line exist = true for d in digit if d not in line exist = false break else line = line replace d 1 return exist 		3 12
def main with open a-small-attempt1.in r as ins numoftestcases = int ins readline cases = for i in range numoftestcases line = ins readline rstrip print case # + str i + 1 + : + process line 		109 120
def removedigit digit line new_line = for d in digit line = line replace d 1 new_line = line return new_line 		15 26
