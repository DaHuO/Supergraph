def to_chars runs result = for x in runs result += chars x return result 		37 41
def instance inst n row = inst counts = result = logging debug row %s % row for x in row split counts append int x logging debug counts logging debug counts total = sum counts while total >= 0 assert max counts > 0 runs = two_arg_max counts logging debug len: %d % len runs for x in runs logging debug senator running: %d % x if total % 2 == 1 runs = runs 1 total -= len runs for x in runs counts x -= 1 if total > 0 result = result + to_chars runs +  else result = result + to_chars runs logging debug counts logging debug counts if max counts > sum counts / 2 print counts print max counts sum counts / 2 assert false if total == 0 break return result 		55 88
def two_arg_max cnts m = max cnts result = cnt = 0 for i in range len cnts if cnts i == m result append i cnt += 1 if cnt >= 2 return result return result 		43 53
