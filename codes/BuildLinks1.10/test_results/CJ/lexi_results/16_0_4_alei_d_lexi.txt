def solve k c s t if c == 1 if s < k sys stdout write case #{0}: impossible\n format tt else sys stdout write case #{0}:{1}\n format tt join  + str x + 1 for x in range k return 0 if k == 1 sys stdout write case #{0}: 1\n format tt return kc = k ** c maxi = - 1 for l in range k - 1 0 - 1 for g in range 1 k l g = l g for i in range c - 1 g = l * g + k * g l = l * l if l + g > kc break if l + g == kc maxi = max maxi l if maxi == - 1 sys stdout write case #{0}: 1\n format tt return if s < maxi sys stdout write case #{0}: impossible\n format tt else sys stdout write case #{0}:{1}\n format tt join  + str x for x in range 2 maxi + 2 		5 27
