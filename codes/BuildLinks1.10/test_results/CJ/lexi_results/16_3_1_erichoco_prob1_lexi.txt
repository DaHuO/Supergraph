def solve senators sen_dict = alphabet i x for i x in enumerate senators sen_list = sorted sen_dict iteritems key = lambda k v - v k sen_dict pop sen_list 0 0 none sen_dict pop sen_list 1 0 none results = top_two_diff = sen_list 0 1 - sen_list 1 1 results += sen_list 0 0 + sen_list 0 0 +  * top_two_diff / 2 if top_two_diff % 2 != 0 results += sen_list 0 0 +  for sen in sen_dict num = sen_dict sen if num / 2 != 0 results += sen * num / 2 * 2 +  if num % 2 != 0 results += sen +  for x in xrange 0 sen_list 1 1 results += sen_list 0 0 + sen_list 1 0 +  return results 		15 43
def main if len sys argv < 2 print please provide input file print usage: %s inputfile [outputfile] % sys argv 0 return timestart = datetime datetime now try inputfile = open sys argv 1 except print failed to read input file %s % sys argv 1 return try outputfile = open sys argv 2 w if len sys argv >= 3 else none except print failed to create output file %s % sys argv 2 return testcases = int inputfile readline strip print ----------------- print test cases: %d % testcases print no output file if len sys argv < 3 else writing to %s % sys argv 2 print ----------------- for testcasenumber in range 1 testcases + 1 n = int inputfile readline strip n_list = map int inputfile readline strip split string = case #%d: %s % testcasenumber solve n_list print string if outputfile outputfile write string + \n print ----------------- print written to %s % sys argv 2 if outputfile else no output file print elapsed time: %s % datetime datetime now - timestart print ----------------- inputfile close if outputfile outputfile close 		45 143
