def minus_all dict str for ch in str dict ch -= 1 		2 4
def do_case strs dict = defaultdict int for ch in strs dict ch += 1 outdigits = list while dict z > 0 minus_all dict zero outdigits append 0 while dict x > 0 minus_all dict six outdigits append 6 while dict g > 0 minus_all dict eight outdigits append 8 while dict h > 0 minus_all dict three outdigits append 3 while dict r > 0 minus_all dict four outdigits append 4 while dict f > 0 minus_all dict five outdigits append 5 while dict v > 0 minus_all dict seven outdigits append 7 while dict w > 0 minus_all dict two outdigits append 2 while dict o > 0 minus_all dict one outdigits append 1 while dict i > 0 minus_all dict nine outdigits append 9 outdigits sort out = str d for d in outdigits outs = for ch in out outs += ch return outs 		6 46
