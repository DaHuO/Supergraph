def main input_filename = input.txt output_filename = output.txt open output_filename wb close with open input_filename r+b as f with open output_filename r+b as g t = int f readline strip for i in range 1 t + 1 n = int f readline strip lists = for j in range 2 * n - 1 lists append map int f readline strip split a = collections counter x for l in lists for x in l b = y for y in a if a y % 2 print case #%d: %s % i  join map str sorted b g write case #%d: %s\n % i  join map str sorted b 		3 18
