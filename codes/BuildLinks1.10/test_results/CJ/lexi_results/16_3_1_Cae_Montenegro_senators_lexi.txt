def solve case numparties numberofsenators outputfile write case # + str case + : global letters outputfile myletters = letters numberofsenators size print myletters totalsenators = np sum numberofsenators while totalsenators > 0 outputfile write  evacuate = while len evacuate < 2 if len evacuate == 1 and np sum numberofsenators == 2 evacuate append none break maxpos = np argmax numberofsenators maxval = numberofsenators maxpos numberofsenators maxpos -= 1 totalsenators -= 1 if maxval == 0 evacuate append none else evacuate append myletters maxpos for element in evacuate if element is not none outputfile write element outputfile write \n 		6 32
