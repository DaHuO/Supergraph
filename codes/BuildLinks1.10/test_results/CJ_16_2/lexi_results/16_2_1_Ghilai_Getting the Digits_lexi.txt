def output_solution solution path lines = with open path wb as f for i sol in enumerate solution lines += Case #{index}: {solution}\n format index = i + 1 solution = sol f writelines lines 		18 23
def get_str_without_letter S digit original = S for letter in digit if letter not in S return NOT_FOUND_DIGIT index = S index letter S = S index + S index + 1 return S 		25 35
def read_file path with open path rb as f lines = f readlines test_cases = line for line in lines 1 for i test_case in enumerate test_cases if test_case endswith \n test_cases i = test_case - 1 print test_cases return test_cases 		8 16
def solve_test_case S letter_index = 0 digits_found = while S current_number = LETTERS letter_index new_S = get_str_without_letter S current_number if new_S == NOT_FOUND_DIGIT letter_index += 1 while letter_index >= len LETTERS bad_digit = digits_found - 1 S = bad_digit + S letter_index = LETTERS index bad_digit + 1 digits_found = digits_found - 1 else S = new_S digits_found append current_number return join str LETTERS index l for l in digits_found 		39 63
