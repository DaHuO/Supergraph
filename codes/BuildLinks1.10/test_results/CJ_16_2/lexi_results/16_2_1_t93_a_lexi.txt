def main t = int raw_input for ti in xrange t inp = raw_input counts = ans = for c in inp counts c = counts get c 0 + 1 for l in l1 l2 l3 for k v in l iteritems n = counts get k 0 if n > 0 ans extend v * n for c in digits v counts c -= n print Case #%d: %s % ti + 1 join map str sorted ans 		8 27
