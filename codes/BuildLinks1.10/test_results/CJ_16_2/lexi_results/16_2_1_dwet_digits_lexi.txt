def do_second c n if counts c ans n += counts c subtract_num counts n counts c 		39 42
def subtract_easy counts ans = defaultdict int for c n in Z 0 X 6 G 8 W 2 U 4 if c in counts ans n += counts c subtract_num counts n counts c return ans 		22 29
def subtract_num counts num times for x in xrange times for c in d num assert c in counts and counts c counts c -= 1 		16 20
