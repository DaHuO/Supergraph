def solve counts acc t if t > 9 return None s = 0 for c in counts keys s += counts c if s == 0 return acc can_subtract = True for c in d t keys if c not in counts or counts c < d t c can_subtract = False if can_subtract tmp_counts = counts copy for c in d t keys tmp_counts c -= d t c ans = solve tmp_counts acc + str t t if ans is None ans = solve counts acc t + 1 return ans else return solve counts acc t + 1 		15 36
