def solve i l = sys stdin readline strip d = collections defaultdict int ans = for c in l d c += 1 for lets let num in DIGITS c = d let ans += c * str num for l in lets assert d l >= c d l -= c print join sorted ans 		6 18
