def dp lo s if lo == len C return 0 0 0 if lo s in m return m lo s p = pow10 len C - 1 - lo i = int C lo if C lo != ? else ? j = int J lo if J lo != ? else ? if i == ? or j == ? if s == 0 		15 54
def addp p i j 		12 12
def mymin a b return 		13 13
