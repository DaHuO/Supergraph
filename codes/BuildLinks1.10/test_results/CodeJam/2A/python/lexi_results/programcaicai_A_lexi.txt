def my_sort s if len s == 1 return s else A = s len s // 2 B = s len s // 2 if A < B return my_sort A + my_sort B else return my_sort B + my_sort A 		21 30
def solve N R P S base_sol = R P S def g s mp = R RS P PR S PS return join mp c for c in s for _ in range N for i in range 3 base_sol i = g base_sol i def my_sort s if len s == 1 return s else A = s len s // 2 B = s len s // 2 if A < B return my_sort A + my_sort B else return my_sort B + my_sort A for i in range 3 d = Counter base_sol i d2 = R R P P S S if all d2 k == v for k v in d items return my_sort base_sol i return IMPOSSIBLE 		6 40
def g s mp = R RS P PR S PS return join mp c for c in s 		10 12
