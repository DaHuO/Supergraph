def quick3 arr start end if end <= start return 0 mid = start + end / 2 median = find_median arr start mid end swap arr start median pivot = arr start i = start count = 0 for j in xrange start end + 1 if arr j < pivot swap arr i + 1 j i += 1 swap arr start i count = end - start count1 = quick3 arr start i - 1 count2 = quick3 arr i + 1 end count = count + count1 + count2 return count 		85 109
def quick2 arr start end if end <= start return 0 swap arr start end pivot = arr start i = start count = 0 for j in xrange start end + 1 if arr j < pivot swap arr i + 1 j i += 1 swap arr start i count = end - start count1 = quick2 arr start i - 1 count2 = quick2 arr i + 1 end count = count + count1 + count2 return count 		57 75
def find_median arr i j k if arr i < arr j if arr i < arr k return j if arr j < arr k else k else return i else if arr i > arr k return k if arr j < arr k else j else return i 		6 16
def swap arr i j temp = arr i arr i = arr j arr j = temp 		1 4
def quick1 arr start end if end <= start return 0 pivot = arr start i = start count = 0 for j in xrange start end + 1 if arr j < pivot swap arr i + 1 j i += 1 swap arr start i count = end - start count1 = quick1 arr start i - 1 count2 = quick1 arr i + 1 end count = count + count1 + count2 return count 		25 49
