def build_max_heap list_ length for j in range 2 length i = j while i > 0 parent_index = int floor i - 1 / 2 if list_ i > list_ parent_index list_ i list_ parent_index = list_ parent_index list_ i i = parent_index else break 		72 89
def max_heapify list_ length index = 0 while index < int floor length - 1 / 2 max_child_index = get_max_child_index list_ index length if list_ index < list_ max_child_index list_ index list_ max_child_index = list_ max_child_index list_ index index = max_child_index else break 		25 40
def get_max_child_index list_ index length left_child = 2 * index + 1 right_child = 2 * index + 2 if left_child > length - 1 return - 1 elif left_child == length - 1 return left_child if list_ left_child > list_ right_child return left_child else return right_child 		43 69
def sort list_ length = len list_ build_max_heap list_ length while length > 0 list_ length - 1 list_ 0 = list_ 0 list_ length - 1 length -= 1 max_heapify list_ length return list_ 		6 22
