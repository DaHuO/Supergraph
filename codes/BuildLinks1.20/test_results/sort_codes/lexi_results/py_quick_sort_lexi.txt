def quicksort alist quicksorthelper alist 0 len alist - 1 		5 6
def quicksorthelper alist first last if first < last splitpoint = partition alist first last quicksorthelper alist first splitpoint - 1 quicksorthelper alist splitpoint + 1 last 		8 14
def partition alist first last pivotvalue = alist first leftmark = first + 1 rightmark = last done = false while not done while leftmark <= rightmark and alist leftmark <= pivotvalue leftmark = leftmark + 1 while alist rightmark >= pivotvalue and rightmark >= leftmark rightmark = rightmark - 1 if rightmark < leftmark done = true else temp = alist leftmark alist leftmark = alist rightmark alist rightmark = temp temp = alist first alist first = alist rightmark alist rightmark = temp return rightmark 		18 46
